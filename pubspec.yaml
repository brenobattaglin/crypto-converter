name: crypto_price_tracker
description: A cryptocurrency price tracker
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  bloc: ^8.0.0
  flutter_bloc: ^8.0.0
  equatable: ^2.0.3
  http: ^0.13.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  bloc_test: ^9.0.1  
  mocktail: ^0.2.0
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true